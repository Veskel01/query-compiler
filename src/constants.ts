export const PATH_SEPARATOR = '.';

export const DEFAULT_INCLUDE_KEY = 'include';
export const DEFAULT_SELECT_KEY = 'select';
export const DEFAULT_SORT_KEY = 'orderBy';

export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc'
}
